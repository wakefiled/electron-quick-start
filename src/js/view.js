export const init = () => {
    const objPlayer = document.getElementById('player');
    const objSnapshotCanvas = document.getElementById('snapshot');
    const objCaptureButton = document.getElementById('capture');
    const objSaveButton = document.getElementById('save');
}